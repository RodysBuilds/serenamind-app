import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId: 'nl.serenamind.app', appName: 'SerenaMind', webDir: 'dist', server: { androidScheme: 'https' } };
export default config;
